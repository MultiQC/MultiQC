name: "PR Report Preview: Build"

on:
  pull_request:
    paths:
      - "multiqc/modules/**"

# Cancel in-progress runs for the same PR
concurrency:
  group: pr-report-build-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  build:
    name: "Build Report"
    runs-on: ubuntu-latest
    timeout-minutes: 10

    # Skip bot PRs
    if: github.actor != 'dependabot[bot]'

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Need full history for diff

      - name: "Detect modified modules"
        id: detect
        run: |
          # Get the list of changed files compared to the base branch
          changed_files=$(git diff --name-only origin/${{ github.event.pull_request.base.ref }}...HEAD)

          # Extract unique module names from changed paths
          modules=$(echo "$changed_files" \
            | grep '^multiqc/modules/' \
            | cut -d'/' -f3 \
            | sort -u \
            | tr '\n' ' ')

          if [ -z "$modules" ]; then
            echo "No module changes detected"
            echo "has_modules=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "Detected modified modules: $modules"
          echo "has_modules=true" >> "$GITHUB_OUTPUT"

          # Build --module flags for multiqc CLI
          module_flags=""
          for mod in $modules; do
            module_flags="$module_flags --module $mod"
          done
          echo "module_flags=$module_flags" >> "$GITHUB_OUTPUT"
          echo "module_list=$modules" >> "$GITHUB_OUTPUT"

      - name: "Set up Python"
        if: steps.detect.outputs.has_modules == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: "Install MultiQC"
        if: steps.detect.outputs.has_modules == 'true'
        run: pip install .

      - name: "Download test data"
        if: steps.detect.outputs.has_modules == 'true'
        uses: actions/checkout@v4
        with:
          repository: MultiQC/test-data
          path: test-data

      - name: "Run MultiQC for modified modules"
        if: steps.detect.outputs.has_modules == 'true'
        run: |
          multiqc \
            --force \
            --verbose \
            --outdir multiqc_report \
            --title "PR #${{ github.event.pull_request.number }} Preview" \
            --comment "Modules: ${{ steps.detect.outputs.module_list }}" \
            ${{ steps.detect.outputs.module_flags }} \
            test-data/data/modules

      - name: "Save PR metadata"
        if: steps.detect.outputs.has_modules == 'true'
        run: |
          mkdir -p multiqc_report/.pr-metadata
          echo "${{ github.event.pull_request.number }}" > multiqc_report/.pr-metadata/pr-number
          echo "${{ github.event.pull_request.head.sha }}" > multiqc_report/.pr-metadata/head-sha
          echo "${{ steps.detect.outputs.module_list }}" > multiqc_report/.pr-metadata/module-list

      - name: "Upload report artifact"
        if: steps.detect.outputs.has_modules == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: multiqc-report-preview
          path: multiqc_report/
          retention-days: 1
